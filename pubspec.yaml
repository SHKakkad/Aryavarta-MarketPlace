name: marketplace_aryavarta
description: NL Aryavarta MarketPlace

version: 2.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  firebase_core: ^0.5.0
  firebase_auth: ^0.18.0+1
  cloud_firestore: ^0.14.0+2
  google_sign_in: ^4.5.3
  firebase_storage: ^4.0.0
  image_picker: ^0.6.7+7
  url_launcher: ^5.6.0
  flutter_automation: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
  - images/
  - assets/